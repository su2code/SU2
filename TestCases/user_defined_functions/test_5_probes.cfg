%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Test case: 5 probes (linear search path, ≤10)
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

SOLVER= NAVIER_STOKES
KIND_TURB_MODEL= NONE
RESTART_SOL= NO

CUSTOM_OUTPUTS= 'probe1 : Probe{PRESSURE}[0.002, 0.002, 0.02]; probe2 : Probe{PRESSURE}[0.003, 0.003, 0.03]; probe3 : Probe{PRESSURE}[0.004, 0.004, 0.04]; probe4 : Probe{PRESSURE}[0.005, 0.005, 0.05]; probe5 : Probe{PRESSURE}[0.006, 0.006, 0.06]'

SCREEN_OUTPUT= INNER_ITER, RMS_DENSITY, probe1, probe2, probe3, probe4, probe5
HISTORY_OUTPUT = ITER, CUSTOM

MACH_NUMBER= 0.1
INIT_OPTION= TD_CONDITIONS
FREESTREAM_OPTION= TEMPERATURE_FS
FREESTREAM_TEMPERATURE= 297.62
REYNOLDS_NUMBER= 600
REYNOLDS_LENGTH= 0.02

REF_ORIGIN_MOMENT_X = 0.00
REF_ORIGIN_MOMENT_Y = 0.00
REF_ORIGIN_MOMENT_Z = 0.00
REF_LENGTH= 0.02
REF_AREA= 0.02

FLUID_MODEL= IDEAL_GAS
GAMMA_VALUE= 1.4
GAS_CONSTANT= 287.87
VISCOSITY_MODEL= CONSTANT_VISCOSITY
MU_CONSTANT= 0.001

MARKER_HEATFLUX= ( y_minus, 0.0 )
MARKER_SYM= ( y_plus )
MARKER_PERIODIC= ( x_minus, x_plus, 0,0,0, 0,0,0, 0.01,0,0 )
MARKER_INLET= ( z_minus, 300.0, 100000.0, 0.0, 0.0, 1.0 )
MARKER_OUTLET= ( z_plus, 99000.0 )
MARKER_PLOTTING= ( y_minus )
MARKER_MONITORING= ( y_minus )
MARKER_ANALYZE= ( z_minus, z_plus )

NUM_METHOD_GRAD= GREEN_GAUSS
CFL_NUMBER= 1e4
CFL_ADAPT= NO
TIME_DISCRE_FLOW= EULER_IMPLICIT

LINEAR_SOLVER= FGMRES
LINEAR_SOLVER_PREC= ILU
LINEAR_SOLVER_ERROR= 0.2
LINEAR_SOLVER_ITER= 5

CONV_NUM_METHOD_FLOW= ROE
MUSCL_FLOW= YES
SLOPE_LIMITER_FLOW= NONE

CONV_RESIDUAL_MINVAL= -11
CONV_STARTITER= 0
INNER_ITER= 5

MESH_FORMAT= BOX
MESH_BOX_LENGTH= (0.01, 0.01, 0.1)
MESH_BOX_SIZE= (9, 17, 65)
